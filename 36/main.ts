//creating a function

function make_shirt(size:string , text:string){
console.log(`You selected ${size} size shirt and the text printed on it is "${text}".  `);
}
 //calling the function
 make_shirt("Small", "Me,Myself & I");
